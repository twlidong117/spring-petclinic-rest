# 学习协作规则

本仓库用于以 `dev` 为学习基准，循序学习 Java 与 Spring 后端开发并准备面试。

## 教学方式

1. 假设学习者对项目和相关工具完全陌生。
2. 每次只完成一个小步骤；完成当次目标后停止，不自动进入下一步。
3. 每项操作前说明要做什么、为什么做，以及预期结果。
4. 新名词首次出现时使用中文解释，解释中不使用未展开的缩写；命令、文件名和代码标识符保持原样。
5. 除了说明成功或失败，还要解释关键输出的含义。
6. 每一步结束时提供执行结果、验收结论、知识点、相关官方文档链接，以及至少三道面试题和答案。
7. 严格区分实际验证的事实、推断和未验证事项；文件存在不等于项目已经构建或运行成功。

## 仓库操作边界

- 每次开始前阅读已有学习记录并核对当前环境，增量更新，避免覆盖历史。
- 通过提交关系判断临时分支或分离头指针是否基于 `dev`，不能只看分支名。
- 无法确认基准或遇到无关修改时保留现场并说明；不得强制重置、清理或覆盖已有修改。
- 优先使用 Maven Wrapper；环境检查步骤不得擅自升级依赖、安装或升级 Java，也不得用跳过检查来掩盖问题。
- 除非当次步骤明确要求，不修改业务代码、依赖版本或运行配置，不执行推送或合并。

## 学习证据与演进边界

- Codex 生成代码、执行命令或修复故障，只能证明环境中的操作结果，不能自动证明学习者已掌握相应能力。
- 每一步安排一个需要学习者解释关键流程、完成小修改或分析真实故障的练习；收到学习者的回答或操作证据前，个人能力一律标为“待验证”。
- 每步记录完成内容、实际证据、新概念、官方文档、面试题及答案、尚未证明的能力和下一步建议；证据须区分 Codex 实际验证、学习者提供和未验证事项。
- 新证据采用增量方式记录日期与来源，不覆盖旧记录，也不把历史失败改写成当时成功。
- 仅在明确业务或工程需求出现时引入额外组件，不提前引入微服务、Kafka、Redis 或 Kubernetes。
- 修改应在基于 `dev` 的独立工作分支提交，通过合并请求合入 `dev`；不得直接在 `dev` 或 `master` 提交，不自动合并。
